#include <iostream>
#include <math.h>
#include <vector>

using namespace std;

int main() {
    int n; cin >> n;
    vector<int> mtns(n);
    for (int i = 0; i < n; ++i) {
        cin >> mtns[i];
    }


    return 0;
}