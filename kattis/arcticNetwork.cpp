#include <iostream>
#include <vector>
#include <utility>

using namespace std;

typedef pair<int, int> pii;

int main() {
    int n; cin >> n;
    while (n--) {
        int s, p; cin >> s >> p;
        while (p--) {
            int x, y; cin >> x >> y;
        }
    }
    return 0;
}