#include <iostream>
#include <utility>
#include <vector>

using namespace std;

double dist(double num) {

}

int main() {
    int n;
    while (cin >> n) {
        if (n == 0) break;

        // keep trying values until within threshold

        for (int i = 0; i < n; ++i) {
            double x, y;
            cin >> x >> y;

        }
    }
    return 0;
}